name: CI

on:
  push:
    branches:
      - main
      - release-*
  pull_request: { }
  workflow_dispatch: { }

permissions:
  contents: read
  pull-requests: write  # for golangci-lint PR annotations
  checks: write

env:
  # Common versions
  GO_VERSION: '1.24'
  GOLANGCI_VERSION: 'v2.11.4'
  DOCKER_BUILDX_VERSION: 'v0.30.1'

jobs:
  detect-noop:
    runs-on: ubuntu-24.04
    outputs:
      noop: ${{ steps.noop.outputs.should_skip }}
    steps:
      - name: Detect No-op Changes
        id: noop
        uses: fkirc/skip-duplicate-actions@v5
        env:
          FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          paths_ignore: '["**.md", "**.png", "**.jpg"]'
          do_not_skip: '["workflow_dispatch", "schedule", "push"]'

  report-breaking-changes:
    runs-on: ubuntu-24.04
    needs: detect-noop
    if: needs.detect-noop.outputs.noop != 'true'
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          submodules: true

      - name: Get modified CRDs
        id: modified-crds
        uses: tj-actions/changed-files@v47
        with:
          files: |
            package/crds/**
      - name: Report breaking CRD OpenAPI v3 schema changes
        if: steps.modified-crds.outputs.any_changed == 'true'
        env:
          MODIFIED_CRD_LIST: ${{ steps.modified-crds.outputs.all_changed_files }}
        run: |
          make crddiff
      - name: Report native schema version changes
        if: ${{ inputs.upjet-based-provider }}
        run: |
          make schema-version-diff

  lint:
    runs-on: ubuntu-24.04
    needs: detect-noop
    if: needs.detect-noop.outputs.noop != 'true'

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          submodules: true

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Find the Go Build Cache
        id: go
        run: echo "cache=$(make go.cachedir)" >> $GITHUB_OUTPUT

      - name: Cache the Go Build Cache
        uses: actions/cache@v5
        with:
          path: ${{ steps.go.outputs.cache }}
          key: ${{ runner.os }}-provider-upjet-supabase-go-${{ hashFiles('**/go.sum') }}
          restore-keys: ${{ runner.os }}-provider-upjet-supabase-go-

      - name: Cache Go Dependencies
        uses: actions/cache@v5
        with:
          path: .work/pkg
          key: ${{ runner.os }}-provider-upjet-supabase-pkg-${{ hashFiles('**/go.sum') }}
          restore-keys: ${{ runner.os }}-provider-upjet-supabase-pkg-

      - name: Vendor Dependencies
        run: make vendor vendor.check

      # We could run 'make lint' but we prefer this action because it leaves
      # 'annotations' (i.e. it comments on PRs to point out linter violations).
      - name: Lint
        uses: golangci/golangci-lint-action@v9
        with:
          version: ${{ env.GOLANGCI_VERSION }}

  check-diff:
    runs-on: ubuntu-24.04
    needs: detect-noop
    if: needs.detect-noop.outputs.noop != 'true'

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          submodules: true

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Install goimports
        run: go install golang.org/x/tools/cmd/goimports@v0.42.0

      - name: Find the Go Build Cache
        id: go
        run: echo "cache=$(make go.cachedir)" >> $GITHUB_OUTPUT

      - name: Cache the Go Build Cache
        uses: actions/cache@v5
        with:
          path: ${{ steps.go.outputs.cache }}
          key: ${{ runner.os }}-provider-upjet-supabase-go-${{ hashFiles('**/go.sum') }}
          restore-keys: ${{ runner.os }}-provider-upjet-supabase-go-

      - name: Cache Go Dependencies
        uses: actions/cache@v5
        with:
          path: .work/pkg
          key: ${{ runner.os }}-provider-upjet-supabase-pkg-${{ hashFiles('**/go.sum') }}
          restore-keys: ${{ runner.os }}-provider-upjet-supabase-pkg-

      - name: Vendor Dependencies
        run: make vendor vendor.check

      - name: Check Diff
        run: make check-diff

  unit-tests:
    runs-on: ubuntu-24.04
    needs: detect-noop
    if: needs.detect-noop.outputs.noop != 'true'

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          submodules: true

      - name: Fetch History
        run: |
          # `git fetch --unshallow` intermittently returns a transient 403 from
          # GitHub; retry so a flaky fetch doesn't red-fail the whole run.
          n=0
          until git fetch --prune --unshallow; do
            n=$((n + 1))
            if [ "$n" -ge 5 ]; then
              echo "git fetch --unshallow failed after $n attempts" >&2
              exit 1
            fi
            echo "unshallow attempt $n failed; retrying in $((n * 10))s" >&2
            sleep $((n * 10))
          done

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Find the Go Build Cache
        id: go
        run: echo "cache=$(make go.cachedir)" >> $GITHUB_OUTPUT

      - name: Cache the Go Build Cache
        uses: actions/cache@v5
        with:
          path: ${{ steps.go.outputs.cache }}
          key: ${{ runner.os }}-provider-upjet-supabase-go-${{ hashFiles('**/go.sum') }}
          restore-keys: ${{ runner.os }}-provider-upjet-supabase-go-

      - name: Cache Go Dependencies
        uses: actions/cache@v5
        with:
          path: .work/pkg
          key: ${{ runner.os }}-provider-upjet-supabase-pkg-${{ hashFiles('**/go.sum') }}
          restore-keys: ${{ runner.os }}-provider-upjet-supabase-pkg-

      - name: Vendor Dependencies
        run: make vendor vendor.check

      - name: Run Unit Tests
        run: make -j2 test

      - name: Publish Unit Test Coverage
        uses: codecov/codecov-action@v5
        with:
          flags: unittests
          files: _output/tests/linux_amd64/coverage.txt

  local-deploy:
    runs-on: ubuntu-24.04
    needs: detect-noop
    if: needs.detect-noop.outputs.noop != 'true'

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          submodules: true

      - name: Fetch History
        run: |
          # `git fetch --unshallow` intermittently returns a transient 403 from
          # GitHub; retry so a flaky fetch doesn't red-fail the whole run.
          n=0
          until git fetch --prune --unshallow; do
            n=$((n + 1))
            if [ "$n" -ge 5 ]; then
              echo "git fetch --unshallow failed after $n attempts" >&2
              exit 1
            fi
            echo "unshallow attempt $n failed; retrying in $((n * 10))s" >&2
            sleep $((n * 10))
          done

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Find the Go Build Cache
        id: go
        run: echo "cache=$(make go.cachedir)" >> $GITHUB_OUTPUT

      - name: Cache the Go Build Cache
        uses: actions/cache@v5
        with:
          path: ${{ steps.go.outputs.cache }}
          key: ${{ runner.os }}-provider-upjet-supabase-go-${{ hashFiles('**/go.sum') }}
          restore-keys: ${{ runner.os }}-provider-upjet-supabase-go-

      - name: Cache Go Dependencies
        uses: actions/cache@v5
        with:
          path: .work/pkg
          key: ${{ runner.os }}-provider-upjet-supabase-pkg-${{ hashFiles('**/go.sum') }}
          restore-keys: ${{ runner.os }}-provider-upjet-supabase-pkg-

      - name: Vendor Dependencies
        run: make vendor vendor.check

      - name: Deploying locally built provider package
        run: make local-deploy
